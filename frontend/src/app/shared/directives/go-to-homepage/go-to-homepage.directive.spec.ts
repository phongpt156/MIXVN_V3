import { GoToHomepageDirective } from './go-to-homepage.directive';

describe('GoToHomepageDirective', () => {
  it('should create an instance', () => {
    const directive = new GoToHomepageDirective();
    expect(directive).toBeTruthy();
  });
});
