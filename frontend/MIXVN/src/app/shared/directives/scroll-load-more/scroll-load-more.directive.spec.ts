import { ScrollLoadMoreDirective } from './scroll-load-more.directive';

describe('ScrollLoadMoreDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollLoadMoreDirective();
    expect(directive).toBeTruthy();
  });
});
